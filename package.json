{
  "name": "pakpharmacybackend",
  "version": "1.0.0",
  "main": "server.js",  // Ensure 'server.js' is the entry point
  "scripts": {
    "start": "node server.js",  // Added to tell Railway how to start the app
    "build": "npm install",     // Optional: Ensures dependencies are installed during build
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.8.1"
  }
}
